# 修订单 02(2026-06-10 晚 · 用户全站走查后的整改令)
> 总纲一句话:**观众要的是"可控、彩色、有讲解、看精选",现在给的是"被动、黑白、没文字、看全集"。**
> 所有窗口共守的四条新铁律,先读:

## 零、四条新铁律(覆盖之前的旧规则)
1. **彩色铁律(推翻旧灰度规则)**:所有作品素材一律彩色直出。删掉一切 grayscale 滤镜、删掉"hover 才恢复彩色"的设计(用户:很丑/晃眼)。黑白只属于展厅本身(底色、文字、边框)。
2. **精选铁律**:全站删除数量统计(38件/13在线/16skill/331张 这类数字一律不出现——"显得我们东西很少,我们都是精挑的")。展示数量全面收缩:网站大图位 6-7 个、索引 20 行、素材河删尾部。
3. **讲解铁律**:每个展示单元必须配中文讲解(一句话定位 + 一行具体说明),文案优先从三个学生的原站文案里选,不许大段空画面连滚(用户:"我一开始让你拆解就是要有文字介绍的")。
4. **丝滑铁律**:所有 pin/scrub 段行程再砍一半(在修订单01的基础上再砍);滚动必须步步有响应,禁止"划半天才碰到下一个东西"。段与段衔接禁止白屏闪帧。

## 一、章节顺序再调(总装层)
```
00 序章 → 01 写真 → 02 作品(网站) → 03 视频(4 AI生视频 + 4 代码视频) → 04 局部作品库(原"已交付",改名) → 05 素材河 → 06 技能 → 07 终幕
```
- "已交付"三个字废弃,改「局部作品库」(总装执行)。
- 看不见的墙挪进 02 章网站展示区内部,不再单独压轴(B 执行)。
- WARHOL 字母幕缩小为 02 章开头的"小屏分隔幕",不占独立大屏(B 执行)。

## 二、给窗口 B(02 作品章大改)
1. **彩色化**:B2/B3/B4/B5/B6 全部去灰度、去 hover 变彩。
2. **章长砍半**:B3 滚轮与 B5 斜置的 scrub 行程各减半;918 转台只转**一圈**(现在两圈半,拖沓);B2 快闪保留但 pin 减半。
3. **WARHOL 改小屏**:字母幕从满屏大段改为章首一条分隔带(约 60vh),hover/键盘点亮逻辑保留。
4. **新首位:「从0到1,是最贵的」live 嵌入**——这是老板本人的作品,源码在 E:\Workspaces\showcase-assets\live-works\视觉效果比较好的\从0到1是最贵的（树林）.html。要求:进入视口自动播放它自己的开场特效(动态,不是截图),做成 02 章网站展示的第一个大位,配一行说明文字。
5. **精选 6-7 个网站大图位**(代替现在的 38 件全集网格),优先用 live iframe 或录屏循环(用户:"学生作品的截图是会动的,不用点进去就自动展示精彩效果"),静态截图降为索引用。入选建议:从0到1(首位)/看不见的墙(live,挪入)/表白网站/把自己交给世界定价/树林首屏/语言的力量(live)/树宁智能剪辑。
6. **删丑清单(从展示删,素材库不删)**:给妈妈的一封信、"看到美的想成为美的"绿色页截图、生成专属饮品链接、旭风、江哥和嫂子相关图。可保留:论活着这件小事、生成专属音频链接。
7. **索引 38→20 行**:只留有预览画面的,没画面的行删掉;**预览图改到右侧固定位置**,不许跟随鼠标(晃眼);live 行保留 ↗。
8. **修白屏闪帧**:B2 快闪切到 B5 斜置之间有一帧白屏,查 z-index/背景缺口。
9. 每个大位配两行中文:一句话定位 + 当时的指令或一句说明(从学生原文案选)。

## 三、给窗口 C(06 技能章扩容)
1. **分屏案例 3 → 5-6 个**,新增三个(素材已备好):
   - **智能剪辑·口播对比**:E:\Workspaces\showcase-assets\c-手册解包\asset-11.mp4 + asset-12.mp4(两版对比,左右或前后);文案方向:一段口播素材,剪辑前后两个样子。
   - **做图工作流**:配图 E:\Workspaces\showcase-assets\video-matrix\skill-imagegen-workflow.png;文案(用户口述,提炼后照用):`人工只做两步:把同一个人的照片放进一个文件夹,发一句固定指令。多视图生成、出图、存档,全部后台自动完成。全程不写提示词。` 产出=杂志级人像。
   - **整首成歌**:song.mp3 + 歌词卡(suno-song-creator),可点播。
   - (可选第6个)整本写真:collage 四张合辑。
2. **其余 skill 的文字详解板块**:6-7 个可视化 skill 之外的所有 skill,做成"文字详解清单"——每个三行:干什么 / 给什么(产出) / 对老板的价值。文案参考 A 学生手册站的写法(转化句式),不许只留英文名。这一块是用户点名缺失的重点。
3. 数据行保留实数风格,但不出现全站统计数字。

## 四、给窗口 D(05 素材河 + 07 终幕)
1. 素材河**全彩色**(删 grayscale 与 hover 恢复逻辑)。
2. **缩短**:删尾部簇,精选好看的(丑图换掉:参考窗口 B 的删丑清单)。
3. **删数字卡**(38/13/16/331 那张整卡删除)。
4. 终幕落款去数字:`截至 2026.06 · 38 件成品 · 还在涨` → `精选 · 还在涨`。
5. 每簇的左下元数据保留(这就是讲解,做得对),可以再多一行中文说明。

## 五、给窗口 E(03 视频矩阵,新章)
1. 结构:**4 个 AI 生视频 + 4 个 AI 代码视频**,两组,每组一屏;布局禁止两组长得一样(用户:"中后部雷同、呆板"),从 11 个参考视频里取不同的灵感(如一组用 2×2 小窗、另一组用横向错落)。
2. 已有素材:
   - AI 生视频 ①:E:\Workspaces\showcase-assets\video-matrix\ai-vid-4-jiang.mp4(14s,「从小姜到老姜」金字大片段,已剪好)——主打位;
   - AI 代码视频 ①:E:\Workspaces\showcase-assets\video-matrix\code-video-愤怒时间.html(代码形式,live 嵌入);
   - AI 代码视频 ②③:demo-clip.mp4(2:59)与 demo-video2.html(形变转场)在 a-手册源码包 assets;
   - 其余位做好看的黑底白描边占位卡(编号+待补录)。
3. **每个视频带自绘进度条**,组标题明确写「AI 生视频」「AI 代码视频」。
4. 代码形式 vs 录屏的结论(总规划已定):**代码形式更好看,但同屏 live 最多 2 个**(8 个同屏全 live 会卡),其余用录屏/导出 mp4。
5. 交付独立 demo 页,总装负责插入 02/03 之间并重编号。

## 六、总装自留(本窗口)
"已交付"改名「局部作品库」;章序重排与编号;E 交付后插章;新素材后续剪辑(视频1/2/3 到货后截前 15 秒,视频3 从 1:13 起截 15 秒,保证画面不截停在动作中间)。
